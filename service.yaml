apiVersion: v1
kind: Service
metadata:
  name: my-udagram-feed-api
  labels:
    run: my-udagram-feed-api
spec:
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  selector:
    app: my-udagram-feed-api
